
{:+!eval;
    {:;CURRENT_USER->roles->admin:}
    {:;CURRENT_USER->roles->dev:}
    {:;CURRENT_USER->roles->moderator:}
    >>
    {:!data; {:flg; require :} >> {:;admin_0yt2sa->path:}/access-false.json :}
:}
